function loopsExample () {

    var func = [];

    // Throw Error
   /* for (const i = 1; i <= 10; i++) {
        func.push(function () {
            console.log(i);
        })
    }

    func.forEach(function (fn) {
        fn();
    });*/
}

loopsExample();